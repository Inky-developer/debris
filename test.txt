fn add_x(x: Int) -> fn(Int) -> Int {
    fn wrapper(y: Int) -> Int {
        x + y
    }
    wrapper
}

let dynamic_int = execute("execute if entity @a");

let add_int = add_x(dynamic_int);
let add_double = add_x(dynamic_int * 2);
dbg(add_int(add_double(5)));

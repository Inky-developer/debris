
fn foo(x: Int) {    
    if x < 0 {
        print("Less than zero!");
        return;
    } else if x == 0 {
        print("Exactly zero");
    };
    print("Not less than zero!");
}

print("Hello, World!");
foo(dyn_int(1));

let a = dyn_int(1);

# returns an int from this loop, continue returns nothing
print(loop {
    if a >= 50 {
        break a * a;
    }

    a = a * 3 + 1;

    if a % 2 == 0 {
        continue;
    }

    a = a / 2;
});
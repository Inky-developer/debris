fn fib(n: Int) -> Int {
    let a = 0;
    let b = 1;

    while n > 1 {
        let temp = a + b;
        a = b;
        b = temp;
        n -= 1;
    }

    b
}

let i = 1;
while i < 15 {
    print(fib(i));

    i += 1;
}

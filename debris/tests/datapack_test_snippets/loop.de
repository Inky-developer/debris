let a = dyn_int(0);
let sum = dyn_int(0);

let result = loop {
    if a >= 100 {
        break sum * sum;
    }

    a = a + 1;

    if a % 2 == 0 {
        continue;
    }

    sum = sum + a;
};

result == 6250000
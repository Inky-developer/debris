
# match on `x` depending on its sign
[this.is.an.attribute]
fn sign(x: Int) -> Int {
    if x < 0 {
        return -1;
    };
    
    if x > 0 {
        return 1;
    };

    print("Exactly zero");
    out 0
}

print("Loaded module foo!");

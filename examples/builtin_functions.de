# `execute` inserts a raw minecraft command
let time = execute("time query daytime");
print(`The current time is: $time`);

# `execute` accepts format strings
execute(`scoreboard players operation foo my_scoreboard = $time`);
# inserts a raw minecraft command
let time = execute("time query daytime");
print(time);

# sets any minecraft score
set_score("daytime", "my_scoreboard", time);

# unlike print(), this function can print any type.
# Here, the function logs null
dbg({});
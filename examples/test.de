mod fib {
    fn fib(x: Int) -> String {
        "Fib"
    }
}

let a = 1;

fn print_sum(a: Int, b: Int) {
    let a = 2;
    print(fib.fib(a + b));
}

# dyn_int prevents the compiler from evaluating everything at compile time
let v1 = dyn_int(5);
let v2 = dyn_int(7);
print_sum(v1, v2);

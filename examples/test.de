let a = dyn_int(0);

loop {
    if a > dyn_int(10) {
        break;
    }

    a = a + dyn_int(1);

    if a % dyn_int(2) == dyn_int(1) {
        continue;
    }
    
    print(a * a);
}

print("Done with the loop!");